# Nouana Planner does not use environment variables at runtime.
# Firebase configuration is loaded from firebase-applet-config.json in the project root.
#
# To use your own Firebase project, replace the values in firebase-applet-config.json
# with your own Firebase web app config from the Firebase Console.
#
# See README.md for full setup instructions.
